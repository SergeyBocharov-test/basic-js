const CustomError = require("../extensions/custom-error");

const chainMaker = {
  getLength() {
  },
  addLink(value) {
  },
  removeLink(position) {
  },
  reverseChain() {
  },
  finishChain() {
  }
};

module.exports = chainMaker;

// Commiting this in order to show that I've at least looked at it
